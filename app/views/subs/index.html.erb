<h1>Listing subs</h1>

<table>
  <tr>
    <th>Project</th>
    <th>Source lang</th>
    <th>Trans lang</th>
    <th>Subtitles</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subs.each do |sub| %>
  <tr>
    <td><%= sub.project_id %></td>
    <td><%= sub.source_lang %></td>
    <td><%= sub.trans_lang %></td>
    <td><%= sub.subtitles %></td>
    <td><%= link_to 'Show', sub %></td>
    <td><%= link_to 'Edit', edit_sub_path(sub) %></td>
    <td><%= link_to 'Destroy', sub, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sub', new_sub_path %>
